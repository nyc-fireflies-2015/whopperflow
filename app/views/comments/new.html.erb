<%= form_for @comment, :html => {:class =>"dark-matter"} do |f| %>
  <%= render partial: "/errors" %>
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
    <input name="type" type="hidden" value="<%= @type %>" />
  </p>

  <p>

    <%= f.submit %>
  </p>
<% end %>
